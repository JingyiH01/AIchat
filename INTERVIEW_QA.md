# AI Chat 项目面试 QA（背诵版）

> 用法：面试前通读 2 遍，标 ⭐ 的是高频必考。每个答案都和你的真实代码对应，被追问时按"代码里是这么写的"来答，不要临时编。

---

## 一、项目整体介绍（开场必背）

### Q：用 1 分钟介绍一下这个项目？

**标准回答：**

> 这是我做的一个 **AI 全栈对话应用**，对标 ChatGPT。前端用 **Vue3 + Vite + Pinia**，后端用 **Node.js + Express 5**，数据库用 **MySQL**。
>
> 核心功能是：**多模型对话、SSE 流式输出、多模态 VLM 图片识别、深色模式**。
>
> 后端我做了几件工程化的事：第一，**BFF 代理层**——API Key 只在服务端，前端不接触密钥；第二，**SSE 流式透传**——把上游模型的分块输出原样转发给前端；第三，**JWT 鉴权 + 令牌桶限流**；第四，**会话持久化**——对话历史存 MySQL，用了连接池、事务和参数化查询。
>
> 一句话概括：**从"前端直连 API 的 Demo"演进成了"前后端分离、有鉴权有持久化的全栈产品"。**

---

## 二、架构设计类（⭐ 高频）

### Q：为什么要把 API Key 从前端移到后端？

**答（分三层）：**

1. **安全**：前端代码对用户完全透明，Key 放在浏览器里，任何 XSS 漏洞或浏览器插件都能读到，等于把付费额度暴露出来。放后端后，Key 只存在于服务端环境变量，前端 DOM 里根本没有这个值——**消灭攻击面**。
2. **产品化**：让用户自己去注册、填 Key 是不现实的；自己持 Key 才能做**限流、计费、审计**。
3. **架构**：这就是 **BFF（Backend For Frontend）** 模式，前端只关心业务数据，第三方对接全部封装在后端。

### Q：什么是 BFF？你的项目里怎么体现的？

**答：** BFF 是"为前端定制的后端层"，前端不直接调第三方，而是调自己的后端，后端再去调第三方。我的项目里，前端所有请求都打到我自己的 `/api/chat`，由后端代理到 SiliconFlow。好处是：密钥安全、能加鉴权限流、能对第三方 API 做统一处理（比如模型白名单）。

### Q：你的项目为什么从"纯前端"改成"前后端分离"？

**答（演进故事）：** 最早是纯前端 Demo，Key 放浏览器 localStorage，对话也存 localStorage。但这是"单机自用"形态。要产品化，三个问题必须解决：Key 泄露风险、数据要跟服务端走（换设备不丢）、要能限流审计。所以我加了 Node 后端。这个改动是**主动的架构决策**，不是被动修补。

---

## 三、Node.js / Express 基础（⭐ 高频）

### Q：说说中间件的洋葱模型。

**答：** 请求进来后，依次经过每一层中间件，每层中间件里 `next()` 之前的代码是"去程"，`next()` 之后的代码是"回程"。像一个洋葱，请求从外往里剥，响应再从里往外套。我的项目里，日志中间件就用到了回程：在 `res.on('finish')` 事件里打印耗时，那是在响应发完后执行的。

### Q：错误处理中间件为什么必须是 4 个参数？

**答：** Express 靠**参数个数**识别一个函数是不是错误处理中间件——`(err, req, res, next)` 四个参数，少一个就不生效。错误处理中间件必须放在所有路由**最后**，这样前面任何一层抛出的错误都会传到这里统一处理。

### Q：你用的是 Express 几？有什么特性你用到了？

**答：** Express 5。一个关键特性是**异步路由里的异常会自动传给错误处理中间件**，不用每个 async 函数手动 try/catch。我在 AI 代理服务里 `throw err`，Express 5 会自动捕获并交给 errorHandler。这是对比 Express 4 的改进（Express 4 里 async 抛错会导致请求挂起）。

### Q：CommonJS 和 ESM 的区别？你项目用哪种？

**答：** 我的后端用的 **ESM**（`"type": "module"`，用 `import/export`）。区别：CommonJS 是 `require/module.exports`，运行时加载、支持动态 require；ESM 是 `import/export`，编译时静态分析、支持 tree-shaking、是官方标准。Node 22 对 ESM 支持已经很好。

---

## 四、SSE 流式（⭐ 必考）

### Q：SSE 和 WebSocket 的区别？为什么用 SSE？

**答：** 核心区别是**方向**：
- **SSE**：单向，服务器 → 客户端持续推送，基于 HTTP，实现简单。
- **WebSocket**：双向，全双工，需要单独协议握手。

AI 对话是"客户端发一次请求，服务器持续返回内容"，**单向流**，用 SSE 更轻量、更合适。而且 SSE 走普通 HTTP，不用额外升级协议，负载均衡、代理都好处理。

### Q：你的流式转发是怎么实现的？延迟会不会变差？

**答：** 后端拿到上游的流式响应后，用 `Readable.fromWeb()` 把 Web 流转成 Node 流，再用 `pipeline` **原样透传**给前端。透传不解析内容，前端解析 SSE 的方式和直连完全一样。
延迟几乎无感：流式是**分块传输**，上游每生成一块立刻转发，首字节时间和直连一样，只多一个网络跳的传输时间。

### Q：转发时遇到过一个什么 bug？怎么解决的？

**答（主动讲，加分）：** 踩过两个坑：
1. `upstream.body.pipe is not a function` —— 因为 Node 原生 `fetch` 返回的 `response.body` 是 **Web 标准的 ReadableStream**，没有 Node 流的 `.pipe()` 方法，要用 `Readable.fromWeb()` 转换。
2. 转发后变成整段 JSON 而不是分块 —— 因为我解构请求体时把 `stream` 字段拿出来了，但转发给上游时忘了加回去，导致上游收到的是非流式请求。

### Q：客户端断连了怎么办？（背压 / 资源泄漏）

**答：** 用 `pipeline` 自动处理背压——前端消费慢就暂停读上游，防止内存堆积。断连时监听 `res.on('close')`，如果响应没正常结束就销毁上游流，防止请求一直挂着泄漏资源。

---

## 五、安全类（⭐ 高频）

### Q：JWT 是什么？和 Session 的区别？

**答：** JWT 是"把状态编码进 token 里"的方案，三段式 `header.payload.signature`，服务端用密钥验证签名就能确认身份，**不用查数据库**，所以叫"无状态"。Session 是服务器存一份状态，靠 sessionId 关联，需要内存或 Redis 存。

**对比**：JWT 天然适合分布式/无状态横向扩展；缺点是 token 一旦签发，主动失效较麻烦（要靠过期时间）。我的项目用 JWT 是因为单机部署、无状态足够。

### Q：token 存哪里？为什么？

**答：** 我存在 localStorage，请求时放在 `Authorization: Bearer <token>` 头里。localStorage 方便（不用管 Cookie 的 SameSite/CSRF），但要注意 XSS 风险——所以我的核心防御是**做好 XSS 防护**（前端 Markdown 渲染做了 HTML 转义），让攻击者根本没法执行脚本去读 token。

### Q：什么是令牌桶限流？为什么用它？

**答：** 令牌桶是：桶有容量 C，系统**匀速**往桶里放令牌（每秒 r 个），请求来了取一个令牌，取到放行，取不到就 429。我的实现是容量 20、每秒补 5 个。
**为什么用它**：它能**允许突发**（桶满时一次放 20 个），又限制长期平均速率，比漏桶（不允许突发）灵活，比固定窗口（临界点突刺）稳定。我手写的，不是用现成库。

### Q：怎么防 SQL 注入？

**答：** 全部用**参数化查询**（`?` 占位符），让数据库把参数当"值"而不是"SQL 片段"处理，从根本杜绝注入。我所有 SQL 都是 `pool.execute('... WHERE id = ?', [id])` 这种形式，绝不字符串拼接。

### Q：密码比对时你做了什么特殊处理？

**答：** 用了 `crypto.timingSafeEqual` **常量时间比较**。普通的 `===` 比较字符串，前缀一旦匹配就会提前返回，攻击者能通过测量响应时间逐字符猜出密码（时序攻击）。timingSafeEqual 无论对错耗时都相同。

### Q：CORS 是什么？你的项目怎么处理的？

**答：** CORS 是浏览器的跨域限制机制。前端（5173）访问后端（3000）属于跨域，后端要声明"允许哪些来源访问"。我用了 **CORS 白名单**，只放行我的前端域名，而不是 `cors()` 裸允许所有域名——否则任何网站都能调我的接口薅我的 Key 额度。

### Q：你的项目怎么防 XSS？

**答：** 前端渲染 AI 返回的 Markdown 时，定制了 Markdown-it 管线，做严格的 **HTML 转义**，防止 AI 返回的恶意脚本被执行。这是很关键的——因为 AI 输出是不可信输入。

---

## 六、数据库类（⭐ 高频）

### Q：你的表怎么设计的？为什么这么分？

**答：** 两张表：`conversations`（会话）和 `messages`（消息），**一对多**关系，消息表用 `conversation_id` 外键关联。这样设计因为一个会话有多条消息，分开存才能高效查询"某个会话的所有消息"，也方便会话级别的操作（删除标题、删整个会话）。

### Q：为什么索引用 `(conversation_id, created_at)` 组合索引？

**答：** 因为最频繁的查询是"查某个会话的消息，按时间排序"——`WHERE conversation_id = ? ORDER BY created_at`。组合索引 `(conversation_id, created_at)` 正好覆盖这个查询的过滤和排序，走索引不用回表。这是**最左前缀**原则的应用。

### Q：为什么用连接池？

**答：** 每次执行 SQL 都新建连接会有三次握手 + 鉴权 + 断开的开销，高并发下很浪费。连接池维护一组**复用连接**，用完 `release` 放回，避免反复建连。我用 `mysql2` 的 `createPool`，配置了 `connectionLimit: 10`。

### Q：事务用在哪？为什么？

**答：** 创建会话时要同时插入"会话"和"第一条消息"两条记录，用事务保证**要么都成功，要么都回滚**，不会出现"有会话但没消息"的脏数据。`beginTransaction → 执行 → commit/rollback`。

### Q：外键的 ON DELETE CASCADE 是干嘛的？

**答：** 删除会话时，数据库自动级联删除它所有消息，不用手动先删消息再删会话，保证数据不残留。

### Q：为什么用 utf8mb4 字符集？

**答：** `utf8mb4` 是完整的 UTF-8，支持 emoji 表情。聊天内容经常有表情，如果用旧的 `utf8`（实际只支持 3 字节），存 emoji 会报错或乱码。这是生产环境的常见坑。

### Q：关系型 vs 非关系型数据库怎么选？

**答：** 我的对话数据是**强结构化**的（会话、消息、角色、时间，有明确外键关系），用关系型 MySQL 合适，能保证一致性、支持复杂查询。如果数据结构灵活、嵌套多、要水平扩展，才会考虑 MongoDB。我这里结构清晰、要事务保证一致性，所以选 MySQL。

---

## 七、前端工程类

### Q：你的路由守卫是干嘛的？

**答：** 用 Vue Router 的 `beforeEach` 钩子，检查 localStorage 里有没有 token，没有就重定向到登录页。这和后端的鉴权中间件**前后呼应**——前端守卫管"页面跳转"，后端中间件管"接口访问"，两层防护。

### Q：axios 拦截器你用来做什么？

**答：** 我在请求拦截器里统一给每个请求加 `Authorization: Bearer <token>` 头。这样每个接口不用手动带 token，一处配置全局生效。拦截器适合做鉴权、日志、统一错误处理这些横切关注点。

### Q：Vue 里状态管理为什么用 Pinia？

**答：** Pinia 是 Vue 3 官方推荐的状态库，比 Vuex 更简洁（没有 mutations，直接用 actions），TypeScript 支持更好，而且和 Vue 3 的 Composition API 契合。我的消息列表、设置、主题都放 Pinia 里管理。

---

## 八、工程化 / 部署（可能被问）

### Q：环境变量你怎么管理的？

**答：** 用 `.env` 存密钥，`.gitignore` 忽略它，另配一份 `.env.example` 模板提交到仓库。这样密钥不进 git，别人拉代码看到 example 就知道要配什么。生产环境的 API 地址、CORS 白名单也都环境变量化，本地和线上用同一套代码。

### Q：你的项目上线了吗？

**答（得体说法）：** 目前是本地跑通、端到端联调过的完整状态。部署我做了一部分准备（API 地址、CORS 都环境变量化了，代码可以直接上云），但考虑到单机资源和演示稳定性，我选择**面试时本地演示**。如果后续有服务器，用 PM2 守护 Node 进程、Nginx 托管前端静态资源就能上线。

### Q：为什么用 concurrently？

**答：** 前后端是两个独立服务，要开两个终端跑。我用 `concurrently` 让根目录 `npm run dev` 一条命令同时起前后端，提升开发体验。这是全栈项目的常见做法。

---

## 九、可能的追问（提前准备）

### Q：如果用户量大了，你的项目哪里最先撑不住？

**答：** 两个点：**令牌桶限流是内存存储**，单机没问题，但多机部署时各机器各自计数就失效了，要换 **Redis** 做分布式限流；**MySQL 单机**扛不住高并发，要做读写分离、分库分表。这说明我有意识知道单机方案的边界。

### Q：流式响应中断了怎么恢复？

**答：** 目前是断连就销毁流、前端提示重试。如果要完善，可以记录对话进度，重连时从断点续传，或者用"最后一次完整消息"做校验。

### Q：图片消息为什么只存文本不存图片？

**答：** VLM 图片是 base64，动辄几 MB，存数据库会让表爆炸。我持久化时只存图片对应的文本部分，图片本身不落库。这是数据量控制的一个取舍。

### Q：模型白名单的作用？

**答：** 后端用 Set 白名单限制前端只能选我允许的模型，防止有人改请求体去调用我没有授权或付费的模型，白嫖额度。这是计费安全。

---

## 十、你亲手踩过的坑（主动讲 = 最强加分）

面试官问"你遇到什么难题"时，讲这些，它们证明你是真做过的：

1. **Web 流 vs Node 流**：`response.body.pipe is not a function`，学会用 `Readable.fromWeb` 转换。
2. **解构丢字段**：流式变成整段 JSON，因为解构 `stream` 后忘了加回 payload。
3. **模型被下架**：写脚本批量探测模型可用性，按返回码区分"余额不足"和"已禁用"，不靠文档假设。
4. **国内网络**：brew 装 MySQL 卡在 ghcr.io，学会给 git/brew 配代理。
5. **git 分叉冲突**：远程和本地分叉，学会 merge 而不是 force push（force 会丢历史）。
6. **跨模型上下文污染**：选了 DeepSeek 却回答"我是 Gemini"——根因是历史消息里旧模型的角色自述压过了新模型的训练记忆（LLM 是"上下文预测"不是"记住身份"，即 in-context learning）。解决：后端注入 **system prompt** 锚定身份。这是 AI 应用特有的坑。
7. **清空对话刷新后历史恢复**：数据存数据库，前端"清空"只清了 UI。解决：清空时调后端删除该会话，保证前端/数据库一致。
8. **前端吞掉真实错误**：catch 里显示笼统的"抱歉发生错误"，导致排障困难。解决：显示 `error.message`，让后端校验错误（如"不支持的模型"）直接可见。
9. **推理模型首 token 延迟**：用 DeepSeek-V4-Flash 回复"你是谁"都要空等 2-3 秒——因为它是推理模型，先输出 `reasoning_content`（思考）再输出答案，前端只显示 `content`，思考期间界面空白。排查：用 curl 计时发现**首字节 0.69s 很快**，问题在"模型思考"，不是网络。解决：① 默认模型换成非推理的 Qwen2.5-7B（秒回）② 加载动画兜底，思考时给用户反馈。
10. **Markdown 双重空白行**：AI 回复每句下面都空一行——`.message-text` 的 `white-space: pre-wrap` 把 Markdown 里的 `\n` 也渲染成换行，叠加 `<p>` 段落间距形成双重空白。解决：`.markdown-body` 内部改 `white-space: normal` + 调行高，让 Markdown 结构自己控制排版。
11. **图片污染上下文导致全部 400**：切到 VLM 模型（Qwen3-VL-32B）传图后，**切回任何模型都返回"上游 API 错误: 400"**。根因：图片消息残留在前端对话上下文里，之后所有请求都带上 base64 图片，而文本模型不支持图片格式 → 400。解决：后端兜底——**非 VLM 模型自动剥离消息里的图片部分，只留文本**，彻底避免前端状态残留影响上游。

---

## 十二、多模态消息格式兼容（重点案例）

### Q：遇到过最典型的"多模态 + 纯文本模型冲突"的 bug 吗？详细讲讲排查过程。

**答（完整案例，建议背下来）：**

**现象**：在设置面板切到 VLM 图片模型（Qwen3-VL-32B）上传一张图片后，**切换回任何模型，发消息全部返回"上游 API 错误: 400"或"服务器内部错误"**。

**排查过程（三步）**：
1. **先怀疑模型**：以为某个模型挂了，逐一直连 SiliconFlow 测 7 个白名单模型，发现都正常 → 排除模型本身问题。
2. **看后端日志**：发现 400 是上游（SiliconFlow）返回的，说明**发出去的请求格式有问题**。
3. **定位根因**：查数据库发现图片消息**并没有持久化**，但问题依旧 → 锁定了**前端运行时状态**。原来传图后，那条包含 base64 图片的 VLM 消息留在前端 `messages` 上下文里，之后每次发消息都把**全部历史**（含图片）发给后端，文本模型收到 `image_url` 结构 → 400。

**解决**：在后端加 `sanitizeMessages`——如果当前请求的模型**不是 VLM 模型**，就把消息数组里 `type: 'image_url'` 的部分剥掉，只保留 `type: 'text'`。这样即使前端状态残留图片，后端也会自动清洗成纯文本，不再报错。

**为什么这么解决**：
- 前端状态不可控（用户各种操作都可能残留），**后端必须做输入兜底**。
- 这也符合 OpenAI 多模态消息规范（`content` 是数组，含 `image_url` 和 `text` 两种 part），不同模型对"哪些 part 支持"要求不同，服务端按模型能力过滤是标准做法。

**面试价值**：这个案例同时展示——**多模态消息格式规范**、**服务端输入校验/兜底思维**、**完整的排查方法论**（先排除模型 → 看日志 → 定位状态残留）。

---

## 十一、加载体验与性能优化（本轮新增）

### Q：为什么有的模型回复快、有的慢？

**答：** 关键区别在**是否推理模型**。推理模型（如 DeepSeek-V4-Flash、DeepSeek-R1）会先输出 `reasoning_content`（思考过程）再输出 `content`（答案），所以"首 token 前有等待"。非推理模型（如 Qwen2.5-7B）直接生成，几乎无思考延迟。

### Q：你怎么判断一个模型是不是推理模型？

**答：** 两种方式：
1. **最可靠（数据驱动）**：看流式响应里有没有 `reasoning_content` 字段——普通模型只有 `content`，推理模型会先返回一长串 `reasoning_content`。
2. **我项目里的方式（枚举）**：因为模型白名单固定且很小，我维护了一个 `REASONING_MODELS` 集合（集中在一处），发送瞬间就能给出提示，不用等流数据。**取舍**：用已知枚举换即时反馈；新增推理模型时同步更新即可。

### Q：加载状态你是怎么做的？为什么不是简单的"正在思考..."？

**答：** 三点改进：
1. **秒显**：发送瞬间就把 assistant 消息的 `loading` 置 true，不等模型返回首 token。
2. **动态**：用 CSS `@keyframes` 做了三个呼吸跳动的圆点（依次延迟 0.2s 形成波浪），传达"模型在工作"而非卡死。
3. **区分文案**：推理模型显示"正在推理中 · 此模型会先思考再作答"，普通模型显示"正在生成"——告诉用户"为什么慢"，降低焦虑。

**面试价值**：这体现我关注**用户感知**而不只是功能正确——同样的等待，静态"思考中"显得卡死，动态动画 + 说明文案显得专业。

---

*这份文档基于你项目的真实实现，背答案时记得和代码对照，被追问细节时答"代码里是这么写的"最有底气。*
